@model SnappyCart.Models.Users
    @{
    ViewBag.Title = "index";
    }



    <h2>index User Profile</h2>

    @* user name
        first name
         last name
         userID?
         products ordered in the past
         current products in cart?
         change password and confirm password

    *@
    <div class="container">
        <div class="userProfile">
            <form>
                @Html.BeginForm("UserProfile","User",FormMethod.Get))
                {
                    @Html.DisplayTextFor(a => a.UserName)
                    <br />
                    @Html.DisplayTextFor(a => a.FirstName)
                    <br />
                    @Html.DisplayTextFor(a => a.LastName)
                }
            </form>
        </div>
    </div>
